# Support API Specs

Tài liệu thiết kế API phục vụ tiếp nhận ý kiến khiếu nại khách hàng.

*   `POST /api/v1/brand/support/tickets`: Tạo yêu cầu hỗ trợ mới.
*   `GET /api/v1/brand/support/tickets`: Xem danh sách các yêu cầu của nhãn hàng.
